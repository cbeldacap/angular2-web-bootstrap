import { LoginComponent } from './login.component';

let app:LoginComponent = new LoginComponent();

describe('LoginComponent', () => {
  beforeEach(() => {
  });

  it('should be defined!', () => {
    expect(true).toBeTruthy();
  });

});
